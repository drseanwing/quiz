/**
 * @file        Constants
 * @description Application-wide constants
 */

export const APP_NAME = import.meta.env.VITE_APP_NAME || 'REdI Quiz Platform';
export const API_URL = import.meta.env.VITE_API_URL || '/api';

// Add more constants as needed
